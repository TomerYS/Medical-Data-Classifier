import os
import pickle
import pandas as pd
import datetime

def predict_and_save(pipeline, X_val, y_val, y_val_pred, train_score, val_score, test):
    # Your code for generating predictions, saving submission file and model here...
